{
  "description": "Jobnik SDK configuration options that can be shared across different components",
  "$id": "https://mapcolonies.com/infra/jobnik/sdk/partial/v1",
  "type": "object",
  "title": "Jobnik worker options v1",
  "required": ["baseUrl"],
  "properties": {
    "baseUrl": {
      "description": "The base URL of the jobnik server",
      "type": "string",
      "format": "uri",
      "x-env-value": "JOBNIK_SDK_BASE_URL"
    }
  }
}
