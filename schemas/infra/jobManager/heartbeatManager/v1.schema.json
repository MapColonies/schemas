{
  "$id": "https://mapcolonies.com/infra/jobManager/heartbeatManager/v1",
  "description": "The heartbeat schema for job manager",
  "type": "object",
  "required": ["url"],
  "properties": {
    "url": {
      "type": "string",
      "description": "The url of heartbeat-manager",
      "format": "uri",
      "x-env-value": "HEARTBEAT_URL"
    }
  }
}
